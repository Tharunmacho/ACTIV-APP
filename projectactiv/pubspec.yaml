name: activ
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # Firebase
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.4
  cloud_firestore: ^4.15.4
  firebase_analytics: ^10.10.4

  # Google & Facebook Auth
  google_sign_in: ^6.2.1
  flutter_facebook_auth: ^6.1.1 # ← Add this for Facebook login
  signin_with_linkedin: ^1.1.0

  # State management
  provider: ^6.1.2

  # Utilities
  shared_preferences: ^2.2.2
  image_picker: ^1.0.0
  flutter_plugin_android_lifecycle: ^2.0.18
  font_awesome_flutter: ^10.1.0 # ← Google & Facebook icons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
